/**
 * Workshop data access.
 * Pages import from here so seed records can be replaced later.
 */
import { seedIdeas, seedInventory, seedProjects } from "@/data/seed-data"

export const ideas = seedIdeas
export const projects = seedProjects
export const inventory = seedInventory
